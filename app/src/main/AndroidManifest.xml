<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.MyApplication">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- LAUNCHER -->
        <activity
            android:name=".TransferMoneyActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            AuthActivity — redirect URI receiver for Custom Tab PKCE.

            KEY FIX: launchMode="singleTask" + android:taskAffinity=""

            WHY singleTask (not singleTop):
              Chrome Custom Tab runs in a DIFFERENT TASK from your app.
              When myapp://callback arrives, Android searches for an existing
              AuthActivity instance. With singleTop it can't find one in the
              Chrome task and creates a NEW instance there — which has no
              verifier and fails. With singleTask + empty taskAffinity,
              AuthActivity lives in its own isolated task. Android always
              finds the correct instance and routes the callback via
              onNewIntent(). The "singleTask clears the task" behaviour only
              applies when it shares taskAffinity with the caller — the empty
              taskAffinity here prevents that entirely.

            android:taskAffinity=""
              Empty string gives AuthActivity its own task affinity, fully
              isolated from both the app task and the Chrome task.

            exported="true"   — required so the OS can deliver the redirect.
            theme=Translucent — activity is invisible; only the Custom Tab shows.
        -->
        <activity
            android:name=".AuthActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="myapp"
                    android:host="callback" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PdfPreviewActivity"
            android:exported="false"
            android:launchMode="standard" />

        <activity
            android:name=".TransferConfirmationActivity"
            android:exported="false"
            android:launchMode="standard"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize" />

        <activity
            android:name=".TransferSuccessActivity"
            android:exported="false"
            android:launchMode="standard" />

        <service
            android:name=".TicThikNotificationInterceptor"
            android:label="TicThik Signature Interceptor"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>